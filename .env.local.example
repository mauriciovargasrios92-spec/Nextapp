# Supabase (opcional para probar el MVP — sin esto, la app corre en modo demo
# con almacenamiento solo en memoria/consola, y usa DEMO_USER_ID).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Claude API (opcional — sin esto, /api/process-dump usa un fallback
# heurístico simple para que puedas probar el flujo completo igual).
ANTHROPIC_API_KEY=
